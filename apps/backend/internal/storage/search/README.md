# Search Storage

Search projection adapter.

Start with the search capabilities available in the chosen Turso/Limbo-compatible engine and keep this package boundary so FTS, Tantivy, or vector-backed hybrid retrieval can be tested without changing domain services.
