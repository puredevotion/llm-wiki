# Clients

Outbound adapters for online services.

Examples include URL fetchers, LLM providers, embedding providers, auth services, notification services, calendars, and future content connectors. Services depend on narrow client interfaces rather than provider SDKs directly.
