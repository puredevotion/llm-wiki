# Repositories

Repository interfaces and persistence composition.

Services depend on repository interfaces. Concrete implementations live under `internal/storage/*` and hide Turso/Limbo, LadybugDB, and search implementation details.
