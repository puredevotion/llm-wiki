# Protobuf Controllers

Thin protobuf/connect transport adapters for mobile and typed clients.

Responsibilities mirror REST controllers. Protobuf handlers should call the same services as REST handlers and should not duplicate business logic.
